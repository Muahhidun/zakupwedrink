# WeDrink Stock Manager Bot

Telegram бот для учета складских остатков и формирования закупок ингредиентов WeDrink.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd /Users/Dom/projects/wedrink-bot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка

Файл `.env` уже создан с токеном бота. Добавьте ID администраторов:

1. Откройте бота @userinfobot в Telegram
2. Получите свой ID и ID коллег
3. Добавьте их в `.env`:

```env
ADMIN_IDS=123456789,987654321,555666777
```

### 3. Импорт данных из CSV

Первый запуск - импортируем товары и остатки из таблицы:

```bash
python import_data.py
```

### 4. Запуск бота локально

```bash
python main.py
```

## 📦 Деплой на Railway

### 1. Создать Git репозиторий

```bash
cd /Users/Dom/projects/wedrink-bot
git init
git add .
git commit -m "Initial commit: WeDrink Stock Manager Bot"
```

### 2. Создать репозиторий на GitHub

```bash
# Создайте репозиторий на github.com
# Затем:
git remote add origin https://github.com/YOUR_USERNAME/wedrink-bot.git
git branch -M main
git push -u origin main
```

### 3. Деплой на Railway

1. Зайдите на railway.app
2. New Project → Deploy from GitHub repo
3. Выберите `wedrink-bot`
4. Railway автоматически обнаружит `Procfile`
5. Добавьте переменные окружения:
   - `BOT_TOKEN` = ваш токен
   - `ADMIN_IDS` = ID пользователей

**⚠️ ВАЖНО:** Перед деплоем на Railway:

1. Запустите `python import_data.py` локально
2. После импорта скопируйте файл `wedrink.db` в Railway (через Railway CLI или Volume)

Или добавьте команду в `Procfile`:

```
release: python import_data.py
worker: python main.py
```

## 📱 Использование бота

### Команды

**Склад:**
- `/stock` - Ввести остатки на сегодня
- `/current` - Текущие остатки

**Заказы:**
- `/order` - Список товаров для закупа (10 дней)
- `/order7` - Заказ на 7 дней
- `/order14` - Заказ на 14 дней

**Отчеты:**
- `/report` - Расход за вчера в ₸
- `/week` - Расход за неделю
- `/analytics` - Аналитика по товарам

### Workflow

1. **Каждое утро:** Администратор вводит `/stock` и вносит остатки
2. **При поставке:** `/supply` (будет добавлено позже)
3. **Планирование закупа:** `/order` - смотрим что заказать
4. **Отчеты:** `/report` или `/week` - расходы в деньгах

## 🔧 Структура проекта

```
wedrink-bot/
├── main.py              # Основной файл бота
├── database.py          # Работа с БД
├── import_data.py       # Импорт из CSV
├── handlers/            # Обработчики команд
│   ├── start.py
│   ├── stock.py
│   ├── orders.py
│   └── reports.py
├── utils/               # Утилиты
│   ├── calculations.py
│   └── import_csv.py
├── requirements.txt
├── .env
└── Procfile
```

## 🐛 Troubleshooting

**Бот не отвечает:**
- Проверьте токен в `.env`
- Убедитесь что бот запущен (`python main.py`)

**Ошибка импорта:**
- Проверьте путь к CSV файлу в `import_data.py`
- Убедитесь что CSV в UTF-8

**Пустой список заказа:**
- Нужна история остатков минимум за 2-3 дня
- Введите остатки через `/stock` несколько раз

## 📝 TODO

- [ ] Добавить команду `/supply` для фиксации поставок
- [ ] Экспорт в Excel (`/export`)
- [ ] Уведомления о заканчивающихся товарах
- [ ] Графики расхода
- [ ] Multi-user ввод остатков

## 🔐 Безопасность

**⚠️ ВАЖНО:** Токен бота - секретная информация!

После настройки:
1. Удалите сообщение с токеном из чата
2. Никогда не публикуйте `.env` в Git (уже в `.gitignore`)
3. Если токен утек - создайте нового бота у @BotFather

## 📞 Поддержка

Вопросы и предложения: @your_telegram
