# Инструкция по развертыванию на Railway

## Что было сделано

### 1. ✅ Миграция на PostgreSQL
- Создана база данных PostgreSQL на Railway
- Импортировано 40 товаров из CSV
- Импортировано 148 записей остатков за 7 дат (17.11 - 01.12.2024)

### 2. ✅ Telegram Mini App
- Создан веб-интерфейс с таблицей для ввода остатков
- Все товары отображаются списком с полями для ввода количества
- Кнопка "Сохранить" внизу экрана
- После сохранения отправляется мини-отчет с цветовой индикацией

### 3. ✅ Интеграция с ботом
- Кнопка "📝 Ввод остатков (форма)" - открывает Mini App
- Кнопка "📝 Ввод остатков (чат)" - последовательный ввод как раньше
- Данные из Mini App автоматически сохраняются в PostgreSQL

## Развертывание на Railway

### Шаг 1: Подготовка проекта

1. Убедитесь, что все изменения закоммичены в Git:
```bash
git add .
git commit -m "Добавлен Telegram Mini App для ввода остатков"
git push
```

### Шаг 2: Создание нового сервиса для веб-приложения

В Railway у вас сейчас есть:
- PostgreSQL база данных
- Один сервис (бот)

Но для Mini App нам нужен публичный URL. Railway автоматически предоставит его.

**Вариант 1: Запуск бота и веб-сервера вместе (рекомендуется)**

1. В Railway откройте ваш существующий сервис
2. Перейдите в Settings → Variables
3. Добавьте переменную окружения:
   ```
   WEB_APP_URL=https://[ваш-домен].up.railway.app
   ```
   (URL появится после первого деплоя в Settings → Domains)

4. В разделе Settings → Domains:
   - Нажмите "Generate Domain"
   - Скопируйте полученный URL
   - Обновите переменную `WEB_APP_URL` на этот URL

5. Ваш `Procfile` уже настроен для запуска обоих сервисов:
   ```
   worker: ./start.sh
   ```

6. Railway автоматически задеплоит проект при следующем push

**Вариант 2: Отдельные сервисы для бота и веб-приложения**

Если хотите разделить бот и веб-сервер:

1. Создайте новый сервис в Railway:
   - Dashboard → New → GitHub Repo
   - Выберите ваш репозиторий

2. Для веб-сервера создайте `Procfile`:
   ```
   web: python webapp/server.py
   ```

3. Для бота используйте:
   ```
   worker: python main.py
   ```

### Шаг 3: Проверка переменных окружения

Для работы полноценного сайта (Панели управления) и защиты сессий убедитесь, что в Railway настроены:

```
BOT_TOKEN=8164823211:AAEiJpCJGheD3_614Bbyn-hnHzOlRPuW4Mw
BOT_USERNAME=Zakupformbot
DATABASE_URL=postgresql://... (ваша ссылка из Railway)
SESSION_KEY=[Случайная строка из 32 знаков]
PORT=5000
```

> [!IMPORTANT]
> **Настройка домена в Telegram**:
> Чтобы кнопка входа на сайте работала, нужно прописать домен вашего сайта в BotFather:
> 1. Откройте @BotFather в Telegram.
> 2. Введите команду `/setdomain`.
> 3. Выберите вашего бота `@Zakupformbot`.
> 4. Введите URL вашего сайта (например: `https://wedrink-app.up.railway.app`).
> 5. **Важно**: Домен должен быть без пути (только основная часть).

### Шаг 4: Тестирование

1. После деплоя на Railway откройте основной URL сайта.
2. Попробуйте войти через кнопку "Log in with Telegram".
3. Если вы Админ — вы увидите Дашборд, Склад и Заказы.
4. Вы всё еще можете использовать старый Mini App внутри бота — он работает параллельно.

1. После деплоя откройте бота в Telegram
2. Нажмите /start - должна появиться новая кнопка "📝 Ввод остатков (форма)"
3. Нажмите на кнопку - откроется Mini App с таблицей товаров
4. Введите количества и нажмите "Сохранить"
5. Бот должен прислать подтверждение и мини-отчет

### Шаг 5: Получение Chat ID для напоминаний

1. Добавьте бота в группу WeDrink
2. Напишите в группе: `/chatid`
3. Скопируйте полученный ID
4. Обновите переменную `REMINDER_CHAT_ID` в Railway

## Структура проекта

```
zakupwedrink/
├── main.py                      # Основной файл бота
├── database_pg.py               # PostgreSQL адаптер
├── handlers/
│   └── stock.py                 # Обработчики остатков + Mini App
├── webapp/
│   ├── server.py                # Веб-сервер (aiohttp)
│   └── templates/
│       └── stock_input.html     # Mini App интерфейс
├── start.sh                     # Скрипт запуска обоих сервисов
├── Procfile                     # Конфигурация Railway
└── requirements.txt             # Зависимости Python
```

## Как работает Mini App

1. **Открытие**: Пользователь нажимает кнопку с WebApp
2. **Загрузка**: Mini App загружает список товаров из `/api/products`
3. **Ввод**: Пользователь вводит количества в форму
4. **Сохранение**: При нажатии "Сохранить":
   - Данные отправляются обратно в бот через `tg.sendData()`
   - Бот получает данные в обработчике `handle_web_app_data`
   - Данные сохраняются в PostgreSQL
   - Генерируется мини-отчет с цветовой индикацией
   - Отчет отправляется пользователю

## Мини-отчет

После сохранения остатков бот отправляет отчет:

```
📊 ОТЧЕТ ПО СКЛАДУ

❗️ СРОЧНО - НЕТ В НАЛИЧИИ:
🔴 • Виноград консервированный: 0 уп. (0 кг) - НЕТ В НАЛИЧИИ
🔴 • Шоколадный сироп: 0 уп. (0 кг) - НЕТ В НАЛИЧИИ

⚠️ КРИТИЧЕСКИЙ ЗАПАС (до 3 дней):
🟠 • Манговое пюре: 3 уп. (3.6 кг) - на 2 дн.

📌 НИЗКИЙ ЗАПАС (4-7 дней):
🟡 • Латте: 6 уп. (6 кг) - на 5 дн.

✅ Все остальные товары в нормальном запасе!
```

## Возможные проблемы

### 1. Mini App не открывается
- Проверьте, что `WEB_APP_URL` указывает на правильный публичный URL
- Убедитесь, что веб-сервер запущен (проверьте логи в Railway)

### 2. Данные не сохраняются
- Проверьте, что `DATABASE_URL` правильно настроен
- Проверьте логи веб-сервера и бота в Railway

### 3. Напоминания не приходят
- Убедитесь, что `REMINDER_CHAT_ID` содержит правильный ID группы
- Проверьте, что бот добавлен в группу и имеет права на отправку сообщений

## Следующие шаги

1. ✅ Деплой на Railway
2. ✅ Тестирование Mini App
3. ✅ Настройка Chat ID для напоминаний
4. 📝 Добавление импорта данных о поставках (если нужно)
5. 📝 Дополнительные функции по запросу

## Техническая информация

- **База данных**: PostgreSQL 17 на Railway
- **Бот**: aiogram 3.13.1
- **Веб-сервер**: aiohttp + aiohttp-cors
- **Mini App**: Vanilla JavaScript + Telegram WebApp API
